import 'package:flutter/material.dart';

class PermisosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hola Permisos'),
      ),
    );
  }
}
